import { createSlice } from "@reduxjs/toolkit"

const initialState = {
    
}
const btShoeShopSlice = createSlice({
    name: 'btShoeShop',
    initialState,
    
    reducers: {
    },

    extraReducers:(builder)=>{}
})

const {reducer,actions}= btShoeShopSlice